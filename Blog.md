### 0. background infomation
由于主包完全是零帧起手，所以在正式开始前查了一些资料包括有心人士的分享和GPT的归纳，了解到做EEG的一个副业还可以是Tony，好神奇（指的是EEG检测...）。这里不过多赘述，直接jump in!

### 1. preprocessing
首先要明白，预处理不应该被视为一个机械的模板化流程，而是应该要思考，怎么处理这组数据，更有利于之后的分析【数据怎么采集，更有利于分析】。  
Take **ASR** for example，它作为Artifact Subspace Reconstruction的方法，本身是基于统计视角的，i.e. 异常信号可能被识别为噪声。如果我们要做患者被试与正常被试的EEG差异分析，这个时候去使用ASR，个人认为可能存疑。  
Now, back to the pipeline, 经过主包白天的学习，大概有了更深的理解，here is the process as follows:
> - **reading**：读取数据，文件格式不同，读取方式有差异【P.S. 具体函数都可以在[MNE官网](https://mne.tools)查看学习】，主包是`.set`文件，故使用`mne.io.read_raw_eeglab(path)`。
  >> tips：读取之后先看一下数据长啥样`mata.plot()` & 有哪些信息`mata.info`。
> - **电极定位**：This action can make computer know where are the different electrical signals from，可以叫作“读懂EEG”。比较直观的一点是，可以绘制彩色的PSD图。对于绝大多数情况，`raw.set_montage("standard_1020")`足矣，当然也可以运用不同的方法，自己设定。
> - **去除坏导**（人工）：首先需要定义什么是“bad channels” ———— 完全无信号、噪声过大（杂乱/振幅大）、漂移（基线漂移）、工频干扰（由交流电源，如50Hz或60Hz电网引起的电气干扰，叠加在有用信号上的周期性噪声）。`通过交互的图片，可以选择坏导`，之后可以用相邻电极信号的平均赋给坏导，减少损失。
> - **filter**：滤波滤的是波的频率，大致有四种方式：高通（去除低频）、低通（去除高频）、band-pass（截取区间内的）、band-stop（去除一段频率区间的信号）。由于主包用的是公开数据，所以就按照作者的处理的方式选择了band-pass，保留了\[0.5Hz, 45Hz]的部分（这一部分$\delta、\thata、\alpha、\beta、\gamma$的波段都有涵盖哦）
> ```python
> raw.load_data() #By default, MNE does not load data into main memory to conserve resources.
> raw.filter(l_freq=0.5, h_freq=45)
> raw.notch_filter(50) #工频去噪
> raw.plot()
> ```

> - **re-referencing**：重新处理参考电极。因为EEG得到的电信号（微伏级）其实是一个相对值，零点就是参考电极，但是参考电极也是电极呀，也可能有噪音什么的，所以这里重新处理一下参考，没毛病。这里选择“**average**” ———— `raw.set_eeg_reference("average")`，从统计的角度，具有规律性的信号不会因为平均而减弱，但随机产生的噪音会。重参考之后，会更加展现数据的真实面貌，但这些体现了预处理EEG，不是为了100%拿到“标准答案”，而是借助数学，极大可能帮我们还原目标信号的实况。
> - **ICA**：independent component analysis，独立成分分析，类似于PCA主成分分析，也是会通过变换坐标，找出相互独立的成分（不再是原来的成分）。也就是说像肌电、眼动、心跳等等，是可以被分出来的，但也是人工，所以或许有实际经验能区辨得清做这个工作会更好，不然就把好信号排掉了。
> ```python
> # ICA
> from mne.preprocessing import ICA
> 
> #注意：要先在环境装 picard 这个包
> ica = ICA(n_components=10, method='picard', random_state=97, max_iter = 'auto')
> ica.fit(raw)
> 
> raw.load_data()
> ica.plot_sources(raw, show_scrollbars=True, block=True, title='请选择要去除的成分')
> plt.show()
> ica.plot_components()
> 
> raw_clean = ica.apply(raw)
> 
> raw.plot(title="原始数据")
> raw_clean.plot(title="ICA清洗后") #对比ICA效果
> ```

> - 分段：这一步也可以在滤波之前，因为数据更少，后续处理时间快，缺点是做ICA不是更方便。
> - ……


欧克，其实这也是主包今日更实际认识到的EEG预处理流程，并且也上手试了，唯一的遗憾是好像不太会认这个数据，就是做ICA的时候不能很确定。好消息是主包已经可以非常顺畅地用prompt搭建环境哩！  
【2026.5.20】

### 2. epochs-cropping
这其实是主包胡诌的名，因为感觉看着像那么回事😅 之所以在提到“分段”之后再单独讲这件事，是因为后续数据都是源于截出来的这部分epochs。  
**那为什么要分段呢？**  
首先，我的任务是探究不同频率的刺激下不同组的被试在接收到刺激的脑电信号改变与差异。又不同频率的刺激是集中几秒给的，受频率的原因，将整块刺激block作为一个整体更便于分析这段时间的刺激对被试的影响。  
其次，既然探究的是睁眼阶段，那必然不能截取闭眼的情况。  
当然，作为真实数据，EEG就不可能按照我们心意去呈现。具体而言，主包用到的数据集还有很多`eyes movement`、`swallowing`、`boundary`... 都是不能用的。  
基于此，一场声势浩大的crop工作就开始了，全人工流水线repeat切片，“我出力您放心”（就是有些费人🥵）
不过问题不只在切哪一段，切多长也需要考量，抛开统计量本身不谈，尽量做到控制变量唯一也是应该的，特别是时候计算sample entropy，时长得统一，毕竟是熵嘛，至于PSD都还好说。  
> 前者是3s的长度，后者控制大于2s。数据的采样率500Hz。

确实记录到这里的时候主包已经回过神了，如果识别不来脑电图，那本次实验于我又有何意义；如果什么原理都一知半解，那和只会处理数据的机器又有什么区别。所以之后的复盘，主包将采用PPT形式。即使是一个小实验，自己从0到1跑了出来，那就再努努力从1到1+吧。

---
### 3. 探索性与验证性分析
> 是的，主包也是后知后觉，这个实验竟然只要求做到统计分析，不涉及ML，更不提DL。罢了罢了，主包会尝试的。  
> 以下是主包的一些体会。
- 任何时候，图形化都不失为一个选择，在你做分析前，请先可视化地看看数据。
- 任何时候都可以重来，数据读取错误 / 变换错了... 但前提是要认真仔细地分析。拿主包这次的例子来说，出结论的时候，发现结论很不寻常，一步一步反推才发现本来应该做的“相对性”没做。【不过这事儿本身也值得探讨，首先因为静息阶段没有`open/close eyes`的标签，主包害怕截错所以并未采用baseline，而是对求出来的各频率的psd和entropy做了归一化，略过了它们的values去看占比。合不合理呢，好像也说得通，因为这样可以避免个体特异性（比如天生脑电信号强一点）；但也不太够，比如受到刺激psd下降，但归一化会模糊下降值，因为它只是得到了结果的占比，感觉更适合组内分析而不是组间。其实这也体现出数学的重要性了，搞得苯人想去学数学和物理了，真的。】
- 一定要随时记录自己的想法，像闪念一样，别让它们溜走。可以是零星的，也可以是有层次的。
- 时刻回过头来看自己的问题，至少不要失掉方向感。
- 如果你感兴趣并且愿意做好，请多多尝试吧！
- 可以（多）和AI交流但要有批判性地，并且别让AI主导，你永远是你项目的主理人。  
【26.6.4 距离上一次干完活已过了3天】
- 虽然你可以对原始数据做各种变形，但毫无疑问，原始数据并非一无是处，它能决定你之后的数据分析是否顺畅，甚至是否可行。所以请重视并好好设计与评估原始数据的采集方案。  
【26.6.6 苯人今日在写实验报告，目前的判断是因为原始数据不给力所以实验任务没有100%完成，又想到之前数据科学的第一节课就是在讲数据采集的可行性，故有此感，寥寥几笔以作提醒】
